# Web Push (VAPID) — generate with: npx web-push generate-vapid-keys
# The PUBLIC_ key is exposed to the client. The private key must stay server-only.
PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:dev@maxess.app
